<Window x:Class="DiaryBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiaryBot"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="550"
        MinHeight="400" MinWidth="550">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="120"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Content="Message: " Grid.Column="0" Margin="10"></Label>
        <TextBox Name="MessageTextBox" Grid.Column="1" Margin="10" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Visible"></TextBox>
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
            </Grid.RowDefinitions>
            <Button Name="SendMessageButton" Click="SendMessageButton_Click" Grid.Row="0" Margin="10" Height="30" VerticalAlignment="Top">Send</Button>
            <Button Name="EditLastMessageButton" Click="EditLastMessageButton_Click" Grid.Row="1" Margin="10" Height="30" VerticalAlignment="Top">Update</Button>
        </Grid>
        
        <Label Content="Token: " Grid.Row="1" Grid.Column="0" Margin="10"></Label>
        <TextBox Name="TokenTextBox" Text="{Binding Source={x:Static local:Config.Instance}, Path=Token, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="10"></TextBox>
        <Button Name="UpdateConfigButton" Click="UpdateConfigButton_Click" Grid.Row="1" Grid.Column="2" Margin="10">Update</Button>

        <Label Content="ChatId: " Grid.Row="2" Grid.Column="0" Margin="10"></Label>
        <TextBox Name="ChatIdTextBox" Text="{Binding Source={x:Static local:Config.Instance}, Path=ChatId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="10"></TextBox>
        
        <Label Content="ReplyMessageId: " Grid.Row="3" Grid.Column="0" Margin="10"></Label>
        <TextBox Name="ReplyMessageIdTextBox" Grid.Row="3" Grid.Column="1" Margin="10">
            <TextBox.Text>
                <Binding Path="ReplyMessageId" Source="{x:Static local:Config.Instance}" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                    <Binding.Converter>
                        <local:NullInt32Converter/>
                    </Binding.Converter>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <local:ErrorLabel x:Name="ErrorMessageLabel" 
                          Content="{Binding Source={x:Static local:Error.Instance}, Path=Message}" 
                          Grid.Row="4" Grid.ColumnSpan="3" 
                          HorizontalContentAlignment="Center"
                          Foreground="Red"
                          FontWeight="Bold">
            <local:ErrorLabel.Style>
                <Style TargetType="{x:Type local:ErrorLabel}" BasedOn="{StaticResource {x:Type Label}}">
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="ContentChanged">
                            <EventTrigger.Actions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0"/>
                                        <DoubleAnimation Duration="0:0:3" BeginTime="0:0:5" Storyboard.TargetProperty="Opacity" From="1.0"
                                            To="0.0"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger.Actions>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </local:ErrorLabel.Style>
        </local:ErrorLabel>

    </Grid>
</Window>
